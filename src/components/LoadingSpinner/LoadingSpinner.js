import {ClimbingBoxLoader} from "react-spinners";

const LoadingSpinner = () => {
    return(
        <ClimbingBoxLoader
            color="#231b56"
            size={30}
        />
    )
};

export {LoadingSpinner};